#安装包
install.packages("ggplot2")

#ctrl+回车  执行选中代码

#查看数据
View(a)
str(a$V1)
str(a$V1)
head(a)

#数学运算
sum(cyp$样本,na.rm =TRUE)
median(cyp$样本,na.rm =TRUE) #中值
mean(cyp$样本,na.rm =TRUE) #均值

plot(density(cyp$`突变率(血浆,59717xjycn1)`,na.rm=TRUE))

#test2
